import 'package:flutter/material.dart';

/// OnJejak brand palette. Lives in shared/ because Explore, Reliability,
/// Reports, and Alerts all need the same colors — not scoped to one module.
class AppPalette {
  static const bg = Color(0xFFFCF7F7);
  static const card = Color(0xFFFFFFFF);
  static const teal = Color(0xFF1E8A78);
  static const tealSoft = Color(0xFFE7F4F1);
  static const ink = Color(0xFF161616);
  static const subtext = Color(0xFF8B8B8B);
  static const label = Color(0xFF9A9A9A);
  static const border = Color(0xFFEDEDED);
  static const red = Color(0xFFD9484D);
  static const redSoft = Color(0xFFFBE6E7);
  static const green = Color(0xFF1E9E5A);
  static const greenSoft = Color(0xFFE3F7EA);
}
